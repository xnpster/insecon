[ ca ]
default_ca	= CA_default

[ CA_default ]
database = index.txt

default_crl_days = 30 
default_md = default
crl_extensions = crl_ext

[ crl_ext ]
authorityKeyIdentifier = keyid:always,issuer:always